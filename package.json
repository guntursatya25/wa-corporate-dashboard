{
  "name": "wa-corporate-dashboard",
  "version": "1.0.0",
  "description": "Corporate WhatsApp dashboard — Baileys multi-session, scheduled & reminder messaging, inbox history",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node test/smoke.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/baileys.js && node --check src/scheduler.js && npm test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@whiskeysockets/baileys": "^7.0.0-rc.14",
    "better-sqlite3": "^13.0.3",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "node-cron": "^4.6.0",
    "pino": "^10.3.1",
    "qrcode": "^1.5.4"
  }
}
